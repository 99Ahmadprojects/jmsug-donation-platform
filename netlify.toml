[build]
  publish = "public"
  functions = "netlify/functions"

[[redirects]]
  # Redirect all /api/* requests to the serverless function named "api"
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200